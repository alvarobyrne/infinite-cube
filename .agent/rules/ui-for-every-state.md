---
trigger: always_on
---

1. For every new state or for every new value in a state object there should be a corresponding `lil-gui` controller unless the contrary is stated
2. the closed and hidden state for the `lil-gui` folders should persist through the `src/uiState.js` state manager